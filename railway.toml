[build]
builder = "nixpacks"

[build.nixpacks]
aptPkgs = ["...", "nodejs", "npm"]

[deploy]
startCommand = "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
